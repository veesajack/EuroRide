const config = {
  API_URL: process.env.REACT_APP_API_URL || 'https://euroride-backend.vercel.app',
  WS_URL: process.env.REACT_APP_WS_URL || 'wss://euroride-backend.vercel.app',
};

export default config;
